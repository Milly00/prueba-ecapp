import { NameColumnTablePipe } from './name-column-table.pipe';

describe('NameColumnTablePipe', () => {
  it('create an instance', () => {
    const pipe = new NameColumnTablePipe();
    expect(pipe).toBeTruthy();
  });
});
